# P2.1 build evidence

Status: `NOT_RUN`

## Host commands

```text
validation/p2-1/scripts/check-evidence.sh:
validation/p2/tests/test_quick_menu.sh:
validation/p2.1/tests/test_bridge.sh:
```

Record compiler, flags, exit status, and the output directory produced by
`scripts/run-validation.sh`.

## Android identity

```text
Feature-on build command:
Feature-off build command:
Feature-on APK:
Feature-on APK SHA256:
Feature-off result:
Gradle/Java/SDK versions:
```

The feature-off configuration check is a regression guard and does not prove
that the feature-on APK contains a working runtime.
